org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official

# Published Maven coordinates for relay-core / relay-ui / relay-call (see build.gradle.kts).
# Bump RELAY_VERSION_NAME for every release published to GitHub Packages.
RELAY_GROUP_ID=dev.relay
RELAY_VERSION_NAME=1.0.0
