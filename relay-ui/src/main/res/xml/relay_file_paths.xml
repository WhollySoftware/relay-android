<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- MessageComposer's camera capture writes here before reading it back as a data: URL and
         discarding it — see RelayChat.kt's launchCamera(). -->
    <cache-path name="relay_camera" path="relay-camera/" />
</paths>
